Kết quả benchmark cho thấy LightGBM có thời gian load dữ liệu khoảng 2.33 giây và thời gian training chỉ khoảng 1.95 giây trên CPU, cho thấy mô hình có tốc độ huấn luyện khá nhanh với dataset 284,807 dòng. Mô hình đạt AUC-ROC 0.9517, thể hiện khả năng phân biệt giao dịch gian lận và hợp lệ tốt. Accuracy đạt 99.89%, tuy nhiên F1-Score đạt 72.73%, Precision 65.57% và Recall 81.63%, cho thấy bài toán mất cân bằng lớp vẫn ảnh hưởng đến hiệu quả phát hiện fraud. Inference một dòng có latency khoảng 1.35 ms, phù hợp cho các ứng dụng yêu cầu phản hồi nhanh. Với 1000 dòng, throughput đạt khoảng 647,144 dòng/giây, cho thấy LightGBM có tốc độ inference rất cao trên CPU. Nhìn chung, CPU đã đủ tốt cho mô hình LightGBM này và không nhất thiết phải sử dụng GPU cho workload benchmark hiện tại.